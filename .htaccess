RewriteEngine On

# exclude /public directory because thats only static asset
# RewriteCond %{REQUEST_URI} ^/public.*$
# RewriteRule ^(.*)$ - [L,NC]
# rewrite all request to the index.php 
# RewriteRule ^(.+)$ index.php [QSA,L]
